const User = require('../model/userModel');

const avatarController = {};

avatarController.setAvatar = () => {
    //find the user
    //reset the avatar and change it in the database
    //set res.locals.avatar to the avatar url
    //return back to apirouter
};

module.exports = avatarController;
